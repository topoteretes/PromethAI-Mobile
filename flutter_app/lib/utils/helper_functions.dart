import 'package:flutter_app/lib/models/goal.dart';
import 'package:flutter_app/lib/models/prompt.dart';

class HelperFunctions {
  static List<Prompt> decomposeThoughts(String thought) {
    // This function will decompose a thought into prompts
    // For now, it just returns an empty list
    return [];
  }

  static bool isGoalReached(Goal goal) {
    // This function will check if a goal is reached
    // For now, it just returns false
    return false;
  }
}